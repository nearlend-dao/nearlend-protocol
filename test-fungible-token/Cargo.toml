[package]
name = "test-fungible-token"
version = "0.1.0"
authors = ["Nearlend DAO Team"]
edition = "2018"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
near-sdk = "=4.0.0-pre.7"
near-contract-standards = "=4.0.0-pre.7"
common = { path = "../common/" }
